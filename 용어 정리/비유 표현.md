# 오라클 메모리 구조와 SQL 처리 흐름 비유 정리

## SGA = 교실 앞 공용 칠판

여러 학생(세션/프로세스)이 함께 사용하는 공간.

여기에 다음과 같은 공용 정보가 저장된다.

- 실행계획
- 캐시 데이터
- 로그 버퍼

특징:

- 모두가 공유
- 자주 사용하는 정보 저장
- 동시에 접근 가능
- 경쟁(Resource Contention) 발생 가능

---

# PGA = 개인 책상

학생 개인만 사용하는 작업 공간.

여기에는 다음 작업들이 들어간다.

- Sort
- Hash Join
- 세션 변수
- 커서 상태

특징:

- 다른 사람과 공유하지 않음
- 작업 종료 시 정리됨
- 접속자 수 증가 시 전체 PGA 사용량 증가

---

# Buffer Cache = 자주 보는 교과서 복사본

원본 데이터는 창고(디스크)에 있다.

하지만 매번 창고에 다녀오면 느리기 때문에
자주 사용하는 페이지를 교실에 복사해 둔다.

즉:

- 디스크 접근 감소
- 성능 향상
- 자주 사용하는 데이터 캐싱

---

# Shared Pool = 수업 자료 보관함

SQL 관련 정보들을 저장하는 공간.

---

## Library Cache = 이미 풀어본 문제 해설집

이전에 실행한 SQL의 실행계획 저장.

같은 SQL이 들어오면 다시 분석하지 않고 재사용한다.

→ Soft Parse 가능

---

# Hard Parsing = 처음 문제 푸는 과정

처음 보는 문제라 다음 작업을 모두 수행해야 한다.

- 문법 검사
- 권한 검사
- 실행계획 생성
- 비용 계산

즉,
처음부터 새롭게 문제를 푸는 과정.

비용이 크다.

---

# Soft Parsing = 기존 해설 재사용

이미 이전에 풀어본 문제라면
기존 실행계획을 재사용한다.

즉:

"전에 풀었던 문제네?"

장점:

- CPU 사용 감소
- 빠른 처리 가능

---

# Redo Log Buffer = 사건 기록 메모장

데이터 변경 내용을 임시로 기록하는 공간.

예시:

- 누가
- 언제
- 무엇을 변경했는지

기록한다.

복구를 위해 매우 중요하다.

---

# Redo Log File = CCTV 저장 서버

Redo Log Buffer 내용을 실제 디스크 파일에 저장한다.

특징:

- 전원이 꺼져도 유지
- 장애 복구 가능

---

# Undo = 되돌리기 노트

데이터 변경 이전 상태를 저장한다.

사용 목적:

- Rollback
- Read Consistency

예시:

"원래 값이 뭐였지?"

를 복구 가능하게 해준다.

---

# DB Buffer Cache Hit = 교실 안에서 바로 찾음

필요한 자료가 이미 교실(Buffer Cache)에 존재.

즉:

- 빠른 조회 가능
- 디스크 접근 불필요

---

# Physical Read = 창고 다녀오기

Buffer Cache에 데이터가 없음.

따라서 실제 디스크에서 데이터를 읽어야 한다.

즉:

- 느림
- 디스크 I/O 발생

---

# Row Source = 조립 라인 작업대

실행계획의 실제 수행 단계.

예시:

- Index Scan
- Full Table Scan
- Sort
- Hash Join
- Nested Loop

각 단계가 데이터를 다음 단계로 전달한다.

---

# Nested Loop Join = 한 명씩 대조

A 테이블의 데이터를 하나씩 꺼내며
B 테이블을 반복 탐색.

비유:

출석부 A 학생 한 명씩 꺼내서
출석부 B 전체를 확인하는 방식.

특징:

- 작은 데이터에 유리
- 인덱스 효율 중요

---

# Hash Join = 해시 맵 생성 후 빠르게 탐색

한쪽 데이터를 미리 해시 테이블로 구성.

이후 빠르게 매칭 수행.

특징:

- 대량 데이터에 강함
- 메모리 사용량 큼

---

# Sort Area(PGA 내부) = 개인 책상 위 정렬 공간

학생이 자료를 이름순으로 정렬하는 공간.

만약 공간이 부족하면?

→ Temp Tablespace 사용

---

# Temp Tablespace = 임시 창고

정렬이나 Hash 작업이 너무 커서
PGA에 담기지 않을 경우 사용하는 임시 디스크 공간.

특징:

- 디스크 사용
- 속도 저하 가능

---

# Optimizer = 문제 풀이 전략 교사

옵티마이저는 SQL을 가장 효율적으로 실행할 방법을 결정한다.

판단 요소:

- 인덱스 사용 여부
- 조인 순서
- Full Scan 여부
- 비용(Cost)

---

# 실행계획 = 문제 풀이 순서표

SQL을 어떤 순서와 방식으로 실행할지 정의한 계획.

예시:

1. 어떤 테이블부터 읽을지
2. 어떤 인덱스를 사용할지
3. 어떤 방식으로 조인할지

---

# SQL 처리 흐름 전체 비유

## 1. SQL 요청 발생

학생이 문제를 제출.

---

## 2. Shared Pool 확인

이전에 풀었던 문제인지 확인.

---

## 3. Hard Parse 또는 Soft Parse 결정

- 처음 보는 문제 → Hard Parse
- 이미 본 문제 → Soft Parse

---

## 4. 실행계획 생성

가장 효율적인 풀이 전략 선택.

---

## 5. Row Source 실행

실제 작업 수행.

- Scan
- Join
- Sort

등 진행.

---

## 6. PGA 사용

개인 작업 공간에서 정렬 및 계산 수행.

---

## 7. Buffer Cache 활용

자주 사용하는 데이터 재사용.

---

## 8. Redo/Undo 기록

데이터 변경 이력 저장 및 복구 대비.

---

# 핵심 요약

| 개념 | 비유 |
|---|---|
| SGA | 공용 칠판 |
| PGA | 개인 책상 |
| Buffer Cache | 자주 보는 교과서 복사본 |
| Shared Pool | 수업 자료 보관함 |
| Hard Parse | 처음 문제 풀이 |
| Soft Parse | 기존 해설 재사용 |
| Redo | 사건 기록 메모 |
| Undo | 되돌리기 노트 |
| Physical Read | 창고 다녀오기 |
| Row Source | 조립 라인 작업대 |
| Nested Loop | 한 명씩 대조 |
| Hash Join | 해시 맵 탐색 |
| Temp Tablespace | 임시 창고 |
| Optimizer | 풀이 전략 교사 |
| 실행계획 | 문제 풀이 순서표 |
